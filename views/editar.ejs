<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Editar ficha - <%= pet.nombre %> - PetQR</title>
</head>
<body>
  <h1>Editar ficha de <%= pet.nombre %></h1>
  <form method="POST" action="/editar/<%= pet.id %>">
    <label>Nombre: <input type="text" name="nombre" value="<%= pet.nombre %>" required /></label><br /><br />
    <label>URL Foto: <input type="url" name="foto_url" value="<%= pet.foto_url %>" /></label><br /><br />
    <label>Teléfono dueño: <input type="tel" name="dueno_tel" value="<%= pet.dueno_tel %>" /></label><br /><br />
    <label>WhatsApp dueño: <input type="tel" name="dueno_whatsapp" value="<%= pet.dueno_whatsapp %>" /></label><br /><br />
    <label>Notas: <textarea name="notas"><%= pet.notas %></textarea></label><br /><br />
    <button type="submit">Guardar cambios</button>
  </form>
  <p><a href="/p/<%= pet.id %>">Volver a la ficha</a></p>
</body>
</html>
